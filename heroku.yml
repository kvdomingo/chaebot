build:
  docker:
    web:
      dockerfile: Dockerfile
      target: prod

run:
  release:
    command:
      - ./docker-release.sh
    image: web
  web: /usr/bin/supervisord -c /bot/supervisord.conf
